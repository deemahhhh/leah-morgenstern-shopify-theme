<section style="padding: calc(var(--header-h) + 120px) var(--gutter) 120px; text-align: center;">
  <span class="eyebrow">404</span>
  <h1 class="h1" style="margin: var(--s-5) auto var(--s-6); max-width: 16ch; font-weight: 300;">This page has gone quiet.</h1>
  <a href="{{ routes.root_url }}" class="btn btn-frame">Return home</a>
</section>
